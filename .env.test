# Test environment — loaded by Bun when NODE_ENV=test
# These are safe placeholder values that prevent accidental connections
# to dev infrastructure. The actual values (random ports for ephemeral
# containers) are set at runtime by applyTestEnv() in the test preload.

NODE_ENV=test

# Port 0 = invalid — fails fast if accidentally used before applyTestEnv()
DATABASE_URL=postgresql://test:test@localhost:0/test
REDIS_URL=redis://localhost:0

# Test-only secrets (not real credentials)
BETTER_AUTH_SECRET=ee351d396c28282c5f729512999c552ee8458fe5cd44ed75a20f6d1f6248f48e

# Test URLs (not used by dev server)
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001
FRONTEND_PORT=3000
BACKEND_PORT=3001

# Centrifugo — disabled in tests
CENTRIFUGO_TOKEN_SECRET=test-centrifugo-secret
CENTRIFUGO_URL=http://localhost:0
